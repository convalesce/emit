pluginManagement {
  repositories {
    gradlePluginPortal()
  }
  plugins {
    id 'com.diffplug.spotless' version '6.25.0'
  }
}

plugins {
  id 'com.diffplug.spotless' version '6.25.0' apply false
}

rootProject.name = 'convalesce-emit'

include 'emit-core'
include 'emit-spark'
