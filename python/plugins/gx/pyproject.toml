[project]
name = "convalesce-emit-gx"
version = "0.1.0"
description = "Forward Great Expectations validation results to Convalesce."
readme = "README.md"
license = "Apache-2.0"
authors = [{ name = "Convalesce" }]
# The host tool is deliberately not a dependency: this installs into an
# existing one, and pinning it would fight that environment's resolution.
requires-python = ">=3.9"
dependencies = ["convalesce-emit~=0.1"]

[project.entry-points."gx.plugins"]
convalesce_emit = "convalesce_emit_gx.action:ConvalesceValidationAction"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/convalesce_emit_gx"]

# Tests live beside the code, as they do across these repos, but a package
# installed into a customer's Airflow has no use for them.
exclude = ["**/test/**"]

[tool.black]
line-length = 81
target-version = ["py39"]
